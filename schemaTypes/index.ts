import {claim} from './claim'
import {instruction} from './instruction'
import {source} from './source'
import {topic} from './topic'

export {claim, instruction, source, topic}

export const schemaTypes = [source, topic, claim, instruction]
